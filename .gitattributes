# Seed CSVs are generated by scripts/seed_lake.py::_write_csv with an explicit
# LF terminator and are byte-compared by scripts/verify_seeds.py (drift gate).
# Pin LF so a Windows checkout cannot flip them to CRLF and trip the gate.
data/seeds/*.csv text eol=lf
data/seeds/_seed_sha.txt text eol=lf
