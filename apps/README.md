# apps/

Application services (V2+). Planned:

- `ingestion-api/` — schedule + trigger connectors over HTTP (Kafka/Airflow in V2)
- `search-api/` — RAG search over the gold corpus (Qdrant)
- `admin-api/` — source registry + lineage + quality dashboards

Not required for the V1 foundation.
